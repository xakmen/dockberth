# Build-time telemetry DSNs (docs/TELEMETRY.md). Leave empty/unset to
# build with crash reporting fully disabled (the default for forks/dev).
# Frontend (@sentry/react):
VITE_SENTRY_DSN=
# Rust side (sentry crate, read via option_env! at compile time):
SENTRY_DSN=
