---
title: What is Dockberth?
description: A Windows desktop app that manages Docker-based local dev environments with presets, local .test domains and zero compose files to write.
sidebar:
  order: 1
---

Dockberth is a free, open-source (MIT) Windows desktop app that manages
Docker-based local development environments. Pick a preset, press **Start**,
open `myapp.test` — Docker does the heavy lifting under the hood, while you
get a clean desktop app on top. No compose files to write, no ports to
memorize, no terminal required.

## The problem it solves

Every local project tends to start the same way: copy a `docker-compose.yml`
from the last one, fix the port collisions, edit the hosts file as admin,
keep three terminal tabs alive. Dockberth replaces that ritual:

1. **Add a project folder** (or create a new one from scratch) and pick a
   preset — WordPress, Laravel, Vendure, or generic PHP/Node.
2. **Press Start.** Dockberth generates the Docker Compose environment,
   builds the containers, and wires the project into a shared reverse proxy.
3. **Open `myapp.test`.** Every project gets its own local domain — run as
   many side by side as you like, they never fight over ports.

## How it works, in one screen

```
your-project/
├── .dockberth/            ← generated by Dockberth (safe to delete)
│   ├── docker-compose.yml
│   ├── app.Dockerfile
│   └── config.json        ← the only file you'd ever edit
└── … your code (never touched)
```

- Dockberth renders a Docker Compose file from a base template plus a
  framework preset and keeps it in `.dockberth/` inside your project.
- A single global **Traefik** container owns ports 80/443 and routes each
  `<name>.test` domain to the right project via Docker labels.
- Hosts-file entries live in a Dockberth-managed block, written only by a
  separate elevated helper — the app itself never runs as admin.
- Projects on WSL2 paths run Compose inside the distro; projects on NTFS
  run it directly on Windows.

Curious about the internals? The
[architecture document](https://github.com/xakmen/dockberth/blob/main/docs/ARCHITECTURE.md)
goes deeper.

## Next steps

- [Requirements](/getting-started/requirements/) — what you need installed.
- [Installation](/getting-started/installation/) — grab the installer.
- [Your first project](/getting-started/first-project/) — from zero to `myapp.test`.
